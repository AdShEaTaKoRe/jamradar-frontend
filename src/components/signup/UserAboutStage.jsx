import React from "react"

const UserAboutStage = ({previousStage}) => (
    <>
    <h1> About me and Submit</h1>
    <button onClick={previousStage}>Back</button>
    <button >Submit</button>
  </>
)

export { UserAboutStage }